import React from 'react';

const Gallery = () => {
  return (
    <section
      id="gallery"
      className="bg-primary flex items-center justify-center"
    >
      <div className="bg-background w-[100px] h-[100px]">Gallery</div>
    </section>
  );
};

export default Gallery;
